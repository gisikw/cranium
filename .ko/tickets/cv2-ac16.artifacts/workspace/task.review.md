cranium-v2 dev shell
  mix test     — run tests
  mix compile  — compile
  iex -S mix   — interactive shell
  just         — list recipes

Elixir Elixir 1.18.4 (compiled with Erlang/OTP 27)
Erlang/OTP 28
Both plan tasks completed correctly:
- The TODO stub is live
- The integration test passes (1 test, 0 failures)
- `mix compile --warnings-as-errors` is clean
- No invariant violations

```json
{"disposition": "continue"}
```
