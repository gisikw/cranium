cranium-v2 dev shell
  mix test     — run tests
  mix compile  — compile
  iex -S mix   — interactive shell
  just         — list recipes

Elixir Elixir 1.18.4 (compiled with Erlang/OTP 27)
Erlang/OTP 28
Based on the ticket title "Persistence: Ecto schemas for conversation history, multi-turn context in Store", this is describing **what to build** — specific code structures (Ecto schemas) for storing conversation history and managing multi-turn context.

This is a feature implementation request with a clear expected output: database schemas and persistence logic.

```json
{"disposition": "route", "workflow": "task"}
```
