cranium dev shell
  just test    — run tests
  just build   — build binary
  just deploy  — build + upgrade
```json
{"disposition": "route", "workflow": "task"}
```

The ticket proposes a specific code change: modify how audio transcriptions are displayed, using threaded replies instead of full conversation replies to reduce clutter. This describes what to build (a feature modification), making it a task rather than a research question or bug report.
