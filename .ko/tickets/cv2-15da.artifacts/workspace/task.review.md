cranium-v2 dev shell
  mix test     — run tests
  mix compile  — compile
  iex -S mix   — interactive shell
  just         — list recipes

Elixir Elixir 1.18.4 (compiled with Erlang/OTP 27)
Erlang/OTP 28
Implementation is complete and correct. All plan tasks done, no deviations, INVARIANTS respected, 6/6 epoch tests pass.

```json
{"disposition": "continue"}
```
