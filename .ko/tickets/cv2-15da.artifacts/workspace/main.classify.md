cranium-v2 dev shell
  mix test     — run tests
  mix compile  — compile
  iex -S mix   — interactive shell
  just         — list recipes

Elixir Elixir 1.18.4 (compiled with Erlang/OTP 27)
Erlang/OTP 28
Reading the ticket title: **"Epoch lifecycle: track saturation via upsert_epoch during inference"**

This describes a specific implementation: tracking saturation during the inference phase using the `upsert_epoch` mechanism. It's prescriptive about *what to build* rather than asking whether something is possible or reporting broken behavior.

```json
{"disposition": "route", "workflow": "task"}
```
